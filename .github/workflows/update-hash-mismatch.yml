name: Update Hash Mismatch

on:
  workflow_dispatch:
    inputs:
      identifier:
        description: Package.Identifier
        required: true
      # urls:
      #   description: URLs space separated
      #   required: false
      version:
        description: Version
        required: true
      # WebsiteURL:
      #   description: WebsiteURL
      #   required: false
      submit:
        description: 'True to auto-submit'
        required: true
        type: boolean
        default: false
      # resolves: 
      #   description: 'resolves this issues'
      #   required: false
      # With:
      #   required: true
      #   type: choice
      #   description: 'WinGetCreate or komac'
      #   default: 'komac'
      #   options:
      #   - komac
      #   - WinGetCreate


permissions:
  contents: read

jobs:
  hash-update:
    name: Hash update ${{ inputs.identifier }} ${{ inputs.version }} -s ${{ inputs.submit }} 
    runs-on: windows-2025

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Update package
        id: update_version
        env:
          GITHUB_TOKEN: ${{ secrets.WINGET_PAT }}
          WINGET_PKGS_FORK_REPO: ${{ vars.WINGET_PKGS_FORK_REPO }}
          INPUTS_IDENTIFIER: ${{ inputs.identifier }}
          INPUTS_VERSION: ${{ inputs.version }}
          #PackageName: ${{ inputs.identifier }}
          #With: ${{ inputs.With }}
          #Submit: ${{ inputs.submit }}
          #resolves: ${{ inputs.resolves }}
        run: .\scripts\Update-HashMismatch.ps1 -Submit $${{ inputs.submit }} -PackageId $env:INPUTS_IDENTIFIER -Version $env:INPUTS_VERSION


